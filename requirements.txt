# harness 도구를 실행하는 데 필요한 Python 런타임 의존성.
#
# 대상: scripts/hype-pr, scripts/repo-governance, scripts/change-impact,
#       scripts/work-discovery, scripts/hype-merge, scripts/escalation,
#       scripts/weekly-harness — 모두 policy/*.yaml 을 읽는다.
#
#   python3 -m pip install -r requirements.txt
#
# consumer 저장소는 이 파일을 벤더로 받지 않는다. hype-pr 의 consumer 사본은
# scripts/hype-pr/pr.py 가 canonical harness checkout 으로 위임(execv)하므로,
# 의존성은 consumer 가 아니라 **harness checkout 의 인터프리터**에 있어야 한다.
#
# scripts/notify/ 는 httpx·jinja2 를 추가로 쓴다 — scripts/notify/README.md 참조.
pyyaml>=6,<7
