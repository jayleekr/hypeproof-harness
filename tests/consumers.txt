# Consumer repos that vendor this harness. One path per line. Lines starting
# with # are ignored. Paths can use ~ (HOME expansion) and ${VAR} substitution.
#
# This file is **maintainer-side only** (sync.sh reads it). The paths below
# reflect Jay's local convention (~/CodeWorkspace/...) and are NOT a
# constraint on members — each member chooses their own workspace path
# (the /onboard-member skill asks them).
#
# To override per-machine for the maintainer running sync.sh:
#   CONSUMER_hypeproof_studio=/abs/path/to/local/clone bash tests/run.sh
# (dashes in repo basename become underscores in env var name)
#
~/CodeWorkspace/hypeproof-studio
~/CodeWorkspace/sediment
~/CodeWorkspace/hypeproof/.claude/worktrees/repos
